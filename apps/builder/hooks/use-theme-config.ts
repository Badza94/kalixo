"use client";

import { useThemeConfig as useThemeContext } from "../contexts/theme-context";

// Re-export the context hook for backward compatibility
export { useThemeConfig } from "../contexts/theme-context";
